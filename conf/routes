# Routes
# This file defines all application routes (Higher priority routes first)
# https://www.playframework.com/documentation/latest/ScalaRouting
# ~~~~

# An example controller showing a sample home page
POST        /marker                 umap.controller.UmapController.addMarker()
POST        /marker/google          umap.controller.UmapController.addMarkerFromGoogle()
GET         /layers/:mapName        umap.controller.UmapController.getLayers(mapName: String)
